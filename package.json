{
  "name": "taskapp",
  "version": "1.0.0",
  "description": "taskApp",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint ./ -f table",
    "lint:fix": "yarn lint --fix"
  },
  "author": "cpalacios",
  "license": "ISC",
  "dependencies": {
    "eslint": "^7.7.0",
    "mongoose": "^5.10.2"
  },
  "devDependencies": {
    "eslint-config": "git+ssh://github.com/criszelaya24/eslint-config"
  }
}
